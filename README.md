.prettierrc は Prettier（コード整形ツール） の設定ファイル＝「コードをどのようなスタイルで自動整形するか」
- semi: true　文の末尾に セミコロンを必ず付ける 設定
- singleQuote: true　文字列は シングルクォート（' '） を使う
- trailingComma: "es5"　ES5 で許される場所（配列・オブジェクトなど）にだけ末尾カンマを付ける
- printWidth: 80　1 行の最大文字数。これを超えると自動で改行。
